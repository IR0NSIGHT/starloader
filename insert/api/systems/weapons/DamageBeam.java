package api.systems.weapons;

import org.schema.game.common.controller.elements.weapon.WeaponUnit;

public class DamageBeam extends Weapon {
    public DamageBeam(WeaponUnit internalWeapon) {
        super(internalWeapon);
    }
}
